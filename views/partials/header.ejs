<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= title %>
    </title>
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        crossorigin="anonymous">
    <!-- Font Awesome (opcional para íconos) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css "
        crossorigin="anonymous">
    <!-- Iconos de Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        main {
            flex: 1 0 auto;
        }
    </style>
</head>

<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4 py-3">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img src="/logo.png" width="160" height="60" alt="Logo BravaBook">
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav align-items-center">
                <li class="nav-item">
                    <a class="nav-link <%= title === 'home' ? 'active' : '' %>" aria-current="page" href="/">Inicio</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link <%= title === 'about' ? 'active' : '' %>" href="/about">Acerca</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= title === 'contact' ? 'active' : '' %>" href="/contact">Contacto</a>
                </li>

                <% if (currentUser && currentUser.role==="admin" ) { %>
                    <li class="nav-item">
                        <a class="nav-link <%= title === 'admin' ? 'active' : '' %>" href="/admin">Administrador</a>
                    </li>
                    <% } %>
                        <% if (currentUser ) { %>

                            <li class="nav-item">
                                <% if (currentUser.role==="admin" ) { %>

                                    <a class="nav-link <%= title === 'admin' ? 'active bg-danger text-white m-3' : 'bg-danger text-white m-3' %>"
                                        href="admin/dashboard">
                                        <%= currentUser.name %>
                                    </a>
                                    <% } else { %>
                                        <a class="nav-link <%= title === 'admin' ? 'active bg-danger text-white m-3' : 'bg-danger text-white m-3' %>"
                                            href="/dashboard">
                                            <%= currentUser.name %>
                                        </a>
                                        <% } %> 
                            </li>
                            <% } %>

                                <li class="nav-item d-flex gap-2">

                                    <% if (!currentUser) { %>
                                        <a class="btn btn-outline-light btn-sm" href="/register">SignIn</a>
                                        <% } %>
                                            <% if (!currentUser) { %>
                                                <a class="btn btn-outline-light btn-sm" href="/login">LogIn</a>
                                                <% } else { %>
                                                    <!-- <a class="btn btn-outline-light btn-sm" href="/logout">LogOut</a> -->
                                                    <% } %>


                                </li>
            </ul>
        </div>
    </div>
</nav>
<main>



    <!-- <% if (!currentUser) { %>
  <a href="/login">Iniciar sesión</a>
  <a href="/register">Registrarse</a>
<% } else if (currentUser.role === 'admin') { %>
  <a href="/admin/dashboard">Panel Admin</a>
<% } else { %>
  <a href="/user/profile">Mi perfil</a>
<% } %> -->